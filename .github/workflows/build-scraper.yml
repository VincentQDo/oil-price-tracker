name: Build database

on:
  push:
    branches:
      - main
    paths:
      - "python-scraper/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          cd ./python-scraper
          docker build -t oil-tracker-scraper:latest .

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image
        run: |
          IMAGE_NAME=ghcr.io/${{ github.repository }}/oil-tracker-scraper:latest
          docker tag oil-tracker-scraper:latest $IMAGE_NAME
          docker push $IMAGE_NAME
